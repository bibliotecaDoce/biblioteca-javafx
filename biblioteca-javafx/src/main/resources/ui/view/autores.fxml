<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>

<VBox spacing="12" alignment="CENTER"
      xmlns:fx="http://javafx.com/fxml"
      fx:controller="ui.controller.AutoresController"
      prefWidth="900" prefHeight="700">

    <Label text="Autores" style="-fx-font-size: 22px;"/>

    <GridPane hgap="10" vgap="10" maxWidth="650">

        <Label text="Nombre:" GridPane.rowIndex="0" GridPane.columnIndex="0"/>
        <TextField fx:id="txtNombre" GridPane.rowIndex="0" GridPane.columnIndex="1"/>

        <Label text="Nacionalidad:" GridPane.rowIndex="1" GridPane.columnIndex="0"/>
        <TextField fx:id="txtNacionalidad" GridPane.rowIndex="1" GridPane.columnIndex="1"/>

        <Label text="Fecha nacimiento:" GridPane.rowIndex="2" GridPane.columnIndex="0"/>
        <DatePicker fx:id="dpFechaNacimiento" GridPane.rowIndex="2" GridPane.columnIndex="1"/>

        <Label text="Activo:" GridPane.rowIndex="3" GridPane.columnIndex="0"/>
        <CheckBox fx:id="chkActivo" GridPane.rowIndex="3" GridPane.columnIndex="1"/>

    </GridPane>

    <HBox spacing="10">
        <Button text="Nuevo" onAction="#onNuevo"/>
        <Button text="Guardar" onAction="#onGuardar"/>
        <Button fx:id="btnEditar" text="Editar" onAction="#onEditar"/>
        <Button fx:id="btnEliminar" text="Eliminar seleccionado" onAction="#onEliminar"/>
        <Button fx:id="btnToggleTabla" text="Ver tabla" onAction="#onToggleTabla"/>
        <Button text="Volver" onAction="#onVolver"/>
    </HBox>

    <Label text="Listado (ListView)" style="-fx-font-weight: bold;"/>
    <ListView fx:id="lstAutores" prefHeight="180" maxWidth="780"/>

    <Label text="Tabla (se muestra con el botón)" style="-fx-font-weight: bold;"/>

    <TableView fx:id="tblAutores" prefHeight="280" maxWidth="780" visible="false" managed="false">
        <columns>
            <TableColumn fx:id="colId" text="ID" prefWidth="60"/>
            <TableColumn fx:id="colNombre" text="Nombre" prefWidth="240"/>
            <TableColumn fx:id="colNacionalidad" text="Nacionalidad" prefWidth="180"/>
            <TableColumn fx:id="colFechaNacimiento" text="Nacimiento" prefWidth="140"/>
            <TableColumn fx:id="colActivo" text="Activo" prefWidth="80"/>
        </columns>
    </TableView>

</VBox>
